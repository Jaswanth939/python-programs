l=[]
n=int(input('Enter size:'))
for i in range(1,n+1):
    x=int(input())
    l.append(x)
for i in range(n):
    for j in range(i+1,n):
        if(l[i]<l[j]):
            temp=l[i]
            l[i]=l[j]
            l[j]=temp
print('elements after sorting in descending order is',l)
